{
  "name": "xylon-ide",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@google/genai": "^1.19.0",
    "jszip": "^3.10.1",
    "marked": "^13.0.2",
    "prismjs": "^1.29.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-simple-code-editor": "^0.13.1"
  },
  "scripts": {
    "start": "echo 'No start script configured for this environment.'",
    "build": "echo 'No build script configured for this environment.'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}